Utility classes that are used by multiple daisy subprojects, but due to
their small size and simplicity don't warant a subproject of their own (yet).

The goal is to keep this subproject as small as possible, and possibly it
will disappear alltogether in the future.