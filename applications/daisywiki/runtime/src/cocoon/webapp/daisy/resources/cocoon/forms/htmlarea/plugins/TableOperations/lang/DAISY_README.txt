Language files placed here are only placeholder for missing or incorrect ones
from HTMLArea (otherwise HTMLArea would fail to initialise).

It is by no means the intention that Daisy would start maintaining HTMLArea
translations.
